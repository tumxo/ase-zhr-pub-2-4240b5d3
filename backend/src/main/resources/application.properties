spring.application.name=booking-service
# Port 8080 ist in der Trainings-Umgebung von code-server (VS Code im Browser) belegt.
server.port=8081
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
